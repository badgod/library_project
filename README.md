# 📚 Library Management System (LMS)

ระบบบริหารจัดการห้องสมุดออนไลน์ (Web-based Application) พัฒนาด้วย PHP และ MySQL รองรับการจัดการทรัพยากรห้องสมุดแบบครบวงจร ทั้งหนังสือรูปเล่ม (Physical Copies) และ E-Books

![PHP](https://img.shields.io/badge/PHP-8.0+-777BB4?style=for-the-badge&logo=php&logoColor=white)
![MySQL](https://img.shields.io/badge/MySQL-Database-4479A1?style=for-the-badge&logo=mysql&logoColor=white)
![Bootstrap](https://img.shields.io/badge/Bootstrap-5-7952B3?style=for-the-badge&logo=bootstrap&logoColor=white)
![jQuery](https://img.shields.io/badge/jQuery-3.7-0769AD?style=for-the-badge&logo=jquery&logoColor=white)

---

## ✨ ฟีเจอร์หลัก (Key Features)

### 📖 การจัดการทรัพยากร (Book Management)
* **Book Titles:** จัดการข้อมูลบรรณานุกรม (ชื่อเรื่อง, ผู้แต่ง, หมวดหมู่, ISBN)
* **Physical Copies:** จัดการตัวเล่มหนังสือ (เพิ่มเล่มซ้ำ, เลขทะเบียน Accession No.)
* **E-Books:** รองรับการอัปโหลดและจัดการไฟล์ PDF
* **Cover Images:** ระบบจัดการรูปปกหนังสือพร้อมปรับขนาดอัตโนมัติ

### 👥 ผู้ใช้งานและสมาชิก (User & Member)
* ระบบสมาชิกและเจ้าหน้าที่ (Admin/Member Roles)
* จัดการข้อมูลส่วนตัวและเปลี่ยนรหัสผ่าน

### 🔄 บริการยืม-คืน (Circulation)
* ระบบยืมหนังสือ (Check-out) และกำหนดวันส่งคืน
* ระบบคืนหนังสือ (Check-in) และคำนวณค่าปรับ (Fines)
* ประวัติการยืม-คืนย้อนหลัง

### 📊 แดชบอร์ด (Dashboard)
* สรุปจำนวนหนังสือ, สมาชิก, และสถิติการยืม
* แสดงสถานะหนังสือ (ว่าง/ถูกยืม) แบบ Real-time

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Backend:** PHP (PDO Driver), MySQL
* **Frontend:** HTML5, CSS3, Bootstrap 5 Admin Template
* **JavaScript Libraries:**
    * jQuery & AJAX (สำหรับการรับส่งข้อมูลแบบ Asynchronous)
    * DataTables (แสดงตารางข้อมูลพร้อม Search/Pagination)
    * SweetAlert2 (แสดง Popup แจ้งเตือนสวยงาม)
    * Chart.js (แสดงกราฟสถิติ)
* **Additional Libraries:**
    * `class.upload.php` (จัดการการอัปโหลดและ Resize รูปภาพ)

---

## ⚙️ การติดตั้ง (Installation)

1.  **Clone Repository** หรือดาวน์โหลดไฟล์โปรเจกต์ไปไว้ที่ Web Server (เช่น XAMPP, Laragon)
    ```bash
    htdocs/library_project/
    ```

2.  **เตรียมฐานข้อมูล (Database)**
    * สร้างฐานข้อมูลชื่อ `library_db` ใน phpMyAdmin
    * Import ไฟล์ SQL จาก: `backupdb/library_db.sql`

3.  **ตั้งค่าการเชื่อมต่อ (Configuration)**
    * ตรวจสอบค่าเชื่อมต่อฐานข้อมูลที่ไฟล์: `config/connectdb.php`
    ```php
    $host = "localhost";
    $username = "root";
    $password = "";
    $dbname = "library_db";
    ```

4.  **ตรวจสอบสิทธิ์โฟลเดอร์ (Permissions)**
    * ตรวจสอบว่าโฟลเดอร์เหล่านี้สามารถเขียนไฟล์ได้ (Write Permission):
        * `assets/images/` (สำหรับรูปปก)
        * `uploads/ebooks/` (สำหรับไฟล์ PDF)

---

## 🔐 บัญชีผู้ใช้งานเริ่มต้น (Default Login)

| Role | Username | Password |
| :--- | :--- | :--- |
| **Admin** | `admin` | *ตรวจสอบในฐานข้อมูล* |
| **Member** | `member` | *ตรวจสอบในฐานข้อมูล* |

> **Note:** รหัสผ่านเริ่มต้นสำหรับการ Reset คือ `123456`

---

## 📂 โครงสร้างโปรเจกต์ (Structure)

```text
library_project/
├── admin/                  # ส่วนจัดการระบบหลังบ้าน (Backend)
│   ├── api/                # API Endpoints (PHP) ที่ถูกเรียกผ่าน AJAX
│   ├── pages/              # หน้าเว็บต่างๆ (Dashboard, Users, Books)
│   ├── services/           # Helper Classes (Upload, Security)
│   └── includes/           # Header, Footer, Sidebar
├── assets/                 # ไฟล์ Static (CSS, JS, Images, Fonts)
├── config/                 # การตั้งค่าระบบและ Database
├── backupdb/               # ไฟล์สำรองฐานข้อมูล (.sql)
└── uploads/                # โฟลเดอร์เก็บไฟล์ที่อัปโหลด (E-books)